<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_select_difficulty"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="brendenbernal.anagram.selectDifficulty"
    android:background="@android:color/holo_blue_dark"
    tools:showIn="@layout/activity_select_difficulty">

    <TextView
        android:text="Select Difficulty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/titleSelectDifficulty"
        android:textSize="36sp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="Play"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonPlaySelect"
        android:background="@android:color/holo_blue_bright"
        android:textSize="24sp"
        android:layout_alignParentBottom="true"
        android:layout_alignEnd="@+id/titleSelectDifficulty"
        android:layout_marginBottom="33dp" />

    <Button
        android:text="Back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@android:color/holo_blue_bright"
        android:id="@+id/buttonBackSelect"
        android:textSize="24sp"
        android:layout_alignBaseline="@+id/buttonPlaySelect"
        android:layout_alignBottom="@+id/buttonPlaySelect"
        android:layout_alignStart="@+id/titleSelectDifficulty" />

    <Spinner
        android:id="@+id/spinnerDifficulty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:drawable/btn_dropdown"
        android:layout_above="@+id/buttonBackSelect"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="45dp" />

</RelativeLayout>
